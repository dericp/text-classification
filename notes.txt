Document type - categories, term-frequencies, length
Category type - term-frequencies, List<Documents> in-category

For each document {
	store category information
		create categories that don't exist
	read document, storing term frequencies into document and categories (and keep overall length)
}

Store categories in a map (name --> object) and documents in a Set



Questions:
Naive bayes vs optimal bayes?  Do we need P(c)?
Laplace smooting - a#{w} is # of distinct words in all categories?
Do we want to remove common words? (the)
